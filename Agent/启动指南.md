# ArkPilot Agent 启动指南

## 项目概述

这是一个基于OpenHarmony的ArkTS应用项目，包含ArkTS前端和C++原生模块。

## 环境要求

### 1. 安装 DevEco Studio
- 下载并安装 [DevEco Studio](https://developer.harmonyos.com/cn/develop/deveco-studio)
- 确保安装OpenHarmony SDK（版本6.1.0）

### 2. 安装 Rust 工具链
项目包含Rust原生模块，需要安装：
- Rust (最新稳定版)
- OpenHarmony Rust target：
  ```bash
  rustup target add aarch64-unknown-linux-ohos
  rustup target add x86_64-unknown-linux-ohos
  ```

### 3. 配置环境变量
确保以下环境变量已设置：
- `OHOS_NATIVE`：指向OpenHarmony Native SDK路径
- `CARGO_HOME`：Rust cargo路径

## 启动步骤

### 方法一：使用 DevEco Studio（推荐）

1. **打开项目**
   - 启动 DevEco Studio
   - 选择 "Open" → 导航到 `D:\code\harmony\ArkPilot\Agent`
   - 等待项目同步完成

2. **配置签名**
   - 项目已包含调试签名配置
   - 如果需要重新生成签名，可以在 "File" → "Project Structure" → "Signing Configs" 中配置

3. **选择设备**
   - 在工具栏选择设备模拟器或连接真实设备
   - 支持ARM64和x86_64架构

4. **构建和运行**
   - 点击工具栏的 "Run" 按钮 (绿色三角形)
   - 或使用快捷键 `Shift+F10`

### 方法二：命令行构建

1. **安装 Hvigor 构建工具**
   ```bash
   npm install -g @ohos/hvigor
   ```

2. **构建项目**
   ```bash
   # 进入项目目录
   cd D:\code\harmony\ArkPilot\Agent
   
   # 构建调试版本
   hvigorw assembleHap
   
   # 构建发布版本
   hvigorw assembleHap --mode release
   ```

3. **安装到设备**
   ```bash
   # 安装hap包到设备
   hdc install entry/build/default/outputs/default/entry-default-unsigned.hap
   ```

### 方法三：使用预配置脚本

项目包含C++原生模块构建脚本：

1. **构建C++原生模块**
   ```bash
   cd entry\src\main\cpp
   build_codex_ohos_host.cmd [RUST_WORKSPACE_DIR] [CARGO_TARGET_DIR] [RUST_PROFILE] [RUST_TARGET_TRIPLE]
   ```

   参数说明：
   - `RUST_WORKSPACE_DIR`: Rust工作空间目录
   - `CARGO_TARGET_DIR`: Cargo目标目录
   - `RUST_PROFILE`: 构建配置（debug/release）
   - `RUST_TARGET_TRIPLE`: 目标平台（aarch64-unknown-linux-ohos / x86_64-unknown-linux-ohos）

## 项目结构

```
Agent/
├── AppScope/                    # 应用全局配置
├── entry/                       # 主模块
│   ├── src/main/
│   │   ├── ets/                # ArkTS源码
│   │   │   ├── backend/        # 后端逻辑
│   │   │   ├── pages/          # 页面
│   │   │   └── entryability/   # 应用入口
│   │   └── cpp/                # C++原生模块
│   └── build-profile.json5     # 模块构建配置
├── build-profile.json5         # 应用构建配置
├── oh-package.json5           # 项目依赖
└── hvigorfile.ts              # 构建脚本
```

## 常见问题

### 1. 构建失败：缺少OpenHarmony SDK
- 确保DevEco Studio已正确安装
- 检查环境变量 `OHOS_NATIVE` 是否正确设置

### 2. Rust模块构建失败
- 确认Rust工具链已安装：`rustc --version`
- 确认OpenHarmony target已添加：`rustup target list | grep ohos`
- 检查 `build_codex_ohos_host.cmd` 中的路径配置

### 3. 签名问题
- 调试版本使用自动生成的调试证书
- 发布版本需要申请正式签名证书

### 4. 设备连接问题
- 确保设备已开启开发者模式
- 使用 `hdc list targets` 检查设备连接状态

## 调试

### 日志查看
```bash
# 查看设备日志
hdc shell hilog
```

### 远程调试
- 在DevEco Studio中启用远程调试
- 使用ArkTS调试器进行断点调试

## 注意事项

1. **项目使用OpenHarmony API 10**
2. **需要HarmonyOS 6.1.0或更高版本**
3. **C++模块需要Rust工具链支持**
4. **建议使用DevEco Studio 4.1或更高版本**

## 快速验证

运行以下命令验证环境：
```bash
# 检查Node.js版本
node --version

# 检查Hvigor版本
hvigor -v

# 检查设备连接
hdc list targets

# 检查Rust工具链
rustc --version
cargo --version
rustup target list | grep ohos
```

如果遇到问题，请检查上述步骤并确保所有依赖项已正确安装。