# 对话链路修复复盘

日期：2026-05-02

## 1. 问题现象

这次对话链路的问题不是单点故障，而是连续暴露了几层问题，症状先后表现为：

1. 应用一打开就崩溃，报 `Cannot read property getStatus of undefined`
2. 修掉崩溃后，前端 WebSocket 连不上内嵌后端
3. 修掉 native 库加载后，API 配置看起来“保存成功”，但实际没有生效
4. API 配置生效后，发送消息立即显示“本轮已完成”，但没有任何 assistant 输出
5. 把旧 stub 换掉后，消息显示“执行中”，但一直没有输出
6. 最后暴露出真实错误：`thread/start transport error remote app-server worker channel is closed`

## 2. 代码架构背景

当前对话链路大致是：

```text
ArkTS UI
  -> CodexBackend.ets
  -> CodexNative.ets
  -> libentry.so
  -> napi_init.cpp
  -> libcodex_ohos_host.so
  -> ohos-host/src/lib.rs
  -> embedded app-server (ws://127.0.0.1:7456)
```

其中：

- `Agent/entry/src/main/ets/backend/CodexBackend.ets`
  负责 ArkTS 侧会话、turn 轮询和消息渲染
- `Agent/entry/src/main/ets/backend/CodexNative.ets`
  负责 ArkTS 到 native 的薄封装
- `Agent/entry/src/main/cpp/napi_init.cpp`
  负责 `libentry.so` 到 Rust host 的桥接
- `codex-main/codex-rs/ohos-host/src/lib.rs`
  负责嵌入式 host、provider 配置、thread/turn 协议适配

## 3. 根因分解

### 3.1 `libentry.so` / `libcodex_ohos_host.so` 打包与 ABI 不一致

早期一轮问题来自 native 库根本没有按目标 ABI 正确进包：

- `libentry.so` 和 `libcodex_ohos_host.so` 不在同一个 ABI 目录
- 某些轮次里 `libentry.so` 在 `arm64-v8a`，`libcodex_ohos_host.so` 只有 `x86_64`
- 结果是：
  - `libentry.so failed to load`
  - `Failed to load libcodex_ohos_host.so`

对应修复：

- 调整 `Agent/entry/build-profile.json5`
- 校正 native 库打包路径
- 重新生成并打包 `libcodex_ohos_host.so`

### 3.2 Provider 配置链路和旧版 UI 不一致

旧版左侧调试窗口只会写单份 provider 配置，但新版系统已经是：

- `config.toml`
- `harmony-provider.json`
- `harmony-provider-catalog.json`

并且 `Settings -> API` 实际读取的是 catalog。结果就是：

- 左侧提示 `Provider config saved`
- 但 Settings 页仍然显示 `0 个 Provider`
- 或者显示旧值

对应修复：

- `EntryAbility.ets` 提前初始化 `codexHome`
- `CodexNative.ets` 改成优先从 `AppStorage` 取真实 `codexHome`
- 左侧调试窗口保存时同步 provider config 和 provider catalog

### 3.3 Rust host 仍然跑的是旧 stub

有一段时间日志一直是：

```text
thread=native-thread
turn=native-turn
rawStatus=completed
messages=[]
```

这不是 app-server 的真实返回，而是 `ohos-host/src/lib.rs` 里旧 stub 的占位返回。

真正原因不是源码没改，而是：

- 新的 `libcodex_ohos_host.so` 已经在 `libcodexhost-builder/out/...` 生成
- 但 `Agent/entry/libs/x86_64/libcodex_ohos_host.so` 仍然是旧文件
- 最终 HAP 打进去的也是旧版 so

对应修复：

- 重新编 Rust host
- 手动确认并覆盖 `Agent/entry/libs/x86_64/libcodex_ohos_host.so`
- 重新打包 HAP

说明：这也暴露出一个额外问题，`libcodexhost-builder/build.bat` 当时虽然打印了 `Install target`，但自动覆盖不稳定，后续需要继续清理这条构建链。

### 3.4 `turn/start` 失败后，前端仍然继续轮询

在换上真实 Rust host 后，日志变成了：

```text
turnStart thread=native-thread-1 turn= rawStatus=failed
turnPoll ... status=inProgress summary=["等待更多事件。"]
```

这里真正的问题是：

- `turn/start` 已经失败
- `turnId` 为空
- 但 `CodexBackend.ets` 仍然调用 `pollTurnUntilCompleted()`
- 所以界面一直显示“执行中”，实际上是在轮询一个不存在的 turn

对应修复：

- `Agent/entry/src/main/ets/backend/CodexBackend.ets`
  - `turn/start` 返回 `failed` 或空 `turnId` 时，直接把这一轮标成失败
  - 不再继续无限轮询
  - 日志增加 `error=...`
- `thread/start` 也不再无脑 fallback 到 `native-thread-*`

### 3.5 `turn/start` 的协议参数不完整

与 TUI 的正式调用链相比，Harmony 侧自己构造的 `TurnStartParams` 缺了几项关键字段：

- `approval_policy`
- `approvals_reviewer`
- `sandbox_policy`

而上游 TUI 会明确发送这些字段。

对应修复：

- `codex-main/codex-rs/ohos-host/src/lib.rs`
  - 新增对 `approval_policy` 的解析
  - 构造 `sandbox_policy`
  - 默认补 `approvals_reviewer = User`
- `codex-main/codex-rs/ohos-host/Cargo.toml`
  - 增加 `codex-utils-absolute-path`
  - 用于 `WorkspaceWrite.writable_roots`

### 3.6 最终根因：RemoteAppServerClient 绑定在临时 Tokio runtime 上

最后一条真实报错是：

```text
threadStart failed: thread/start transport error remote app-server worker channel is closed
```

这条错误说明：

- `RemoteAppServerClient` 已经创建成功
- 但它内部 worker task 已经死掉
- 之后再发 `thread/start` 时，请求通道已经关闭

最终定位到 `ohos-host/src/lib.rs` 的实现问题：

- `with_runtime_result(...)` 每次调用都会新建一个临时 Tokio runtime
- `connect_remote_client_if_needed()` 在这个临时 runtime 中创建 `RemoteAppServerClient`
- `RemoteAppServerClient` 内部会 `tokio::spawn` 一个 worker task
- 当 `with_runtime_result(...)` 返回时，这个临时 runtime 被销毁
- worker task 随 runtime 一起结束
- 但 `NativeConversationState.client` 里还保留了这个 client 对象
- 所以后续 `thread/start` / `turn/start` 看到的就是：
  - `remote app-server worker channel is closed`

这就是最后的决定性根因。

对应修复：

- `codex-main/codex-rs/ohos-host/src/lib.rs`
  - 新增全局静态 runtime：
    - `NATIVE_ASYNC_RUNTIME`
  - `with_runtime_result(...)` 改为复用同一个长期存活的 Tokio runtime
  - 不再每次请求临时创建/销毁 runtime

## 4. 关键改动文件

这次修复主要落在这些文件：

- `Agent/entry/build-profile.json5`
- `Agent/entry/src/main/cpp/codex_ohos_host.h`
- `Agent/entry/src/main/cpp/napi_init.cpp`
- `Agent/entry/src/main/ets/backend/CodexBackend.ets`
- `Agent/entry/src/main/ets/backend/CodexNative.ets`
- `Agent/entry/src/main/ets/entryability/EntryAbility.ets`
- `Agent/entry/src/main/ets/pages/Index.ets`
- `codex-main/codex-rs/ohos-host/Cargo.toml`
- `codex-main/codex-rs/ohos-host/src/lib.rs`

## 5. 修复后的行为变化

修复后，日志和行为经历了这些关键变化：

1. 不再出现旧 stub 的：

```text
thread=native-thread
turn=native-turn
rawStatus=completed
messages=[]
```

2. `turn/start` 失败时，不再无限显示“执行中”

3. `RemoteAppServerClient` 不再因为 runtime 被销毁而丢失 worker

4. 对话链路恢复到真实 app-server 驱动，而不是本地假返回

## 6. 经验总结

### 6.1 嵌入式异步客户端不能依赖短生命周期 runtime

这是这次最关键的经验。

只要对象内部持有：

- `tokio::spawn` 出来的任务
- channel worker
- websocket 读写循环

就不能把它创建在“调用完即销毁”的临时 runtime 上。

### 6.2 原生桥接层必须避免“看起来成功，实际上失败”

这次调试最耗时的一个原因是，早期很多错误都被吞掉了，例如：

- provider 保存失败但 UI 还显示 saved
- `turn/start` 失败但 UI 仍然轮询

后续桥接层要尽量遵守：

- native 失败就直接返回明确错误
- ArkTS 层不要再擅自构造“假成功”状态

### 6.3 构建链必须可验证“真正进包的 so 是哪一个”

这次还有一个非常现实的问题：

- Rust so 明明编出来了
- 但 HAP 里仍然是旧文件

以后排查这类问题时，应该第一时间核对：

- builder 输出文件时间戳
- `Agent/entry/libs/<abi>/...` 时间戳
- HAP 里的最终文件

## 7. 后续建议

建议后续继续补三件事：

1. 修正 `libcodexhost-builder/build.bat` 的自动安装行为
   - 确保 builder 成功后，一定覆盖 `Agent/entry/libs/...`

2. 在 `CodexBackend.ets` 里保留当前这套失败快速暴露机制
   - 不要再回退到静默 fallback

3. 给 `ohos-host` 增加更清晰的 turn/start 错误日志
   - 例如在 Rust 侧直接打印最终发给 app-server 的关键字段

## 8. 当前结论

这次 bug 的最终根因不是单纯的“模型没回”或者“WebSocket 不通”，而是多个问题叠加：

- native 库打包/ABI 问题
- provider 配置链路不一致
- 旧 Rust stub 未真正进包
- `turn/start` 失败后 UI 仍然盲轮询
- 最终决定性问题是：
  - `RemoteAppServerClient` 绑定在临时 Tokio runtime 上
  - runtime 结束后 worker channel 被关闭
  - 导致后续 `thread/start` / `turn/start` 无法工作

修完这些后，对话链路才真正恢复可用。
